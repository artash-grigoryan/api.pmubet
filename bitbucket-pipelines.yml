# This is a sample build configuration for Docker.
# Check our guides at https://confluence.atlassian.com/x/O1toN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: agrigoryan/laravel:1.0

pipelines:
  default:
    - step:
        deployment: production
        services:
          - docker
        script: # Modify the commands below to build your repository.
          # Set $DOCKER_HUB_USERNAME and $DOCKER_HUB_PASSWORD as environment variables in repository settings
          - export IMAGE_NAME=agrigoryan/laravel:$BITBUCKET_COMMIT
          - export LATEST_IMAGE_NAME=agrigoryan/laravel:latest
          - docker pull LATEST_IMAGE_NAME
          # build the Docker image (this will use the Dockerfile in the root of the repo)
          - docker build -t $IMAGE_NAME -f ./docker/apache/Dockerfile .
          # authenticate with the Docker Hub registry
          - docker login --username $DOCKER_HUB_USERNAME --password $DOCKER_HUB_PASSWORD
          - docker run --rm -v $(pwd):/var/www/dzio_api -w /var/www/dzio_api $IMAGE_NAME bash -c "composer install;cp .env.example .env;"
          # push the new Docker image to the Docker registry
          - docker push $IMAGE_NAME
          - docker push $LATEST_IMAGE_NAME
